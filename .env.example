# Optional: Cesium Ion access token for 3D terrain + satellite imagery
# Get a free token at https://ion.cesium.com
# Without a token, the app uses OpenStreetMap tiles (still looks great!)
VITE_CESIUM_ION_TOKEN=
